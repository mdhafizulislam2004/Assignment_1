const AppDescription = ({description}) => {
    return (
        <div className="my-3">
            <h1 className="mx-8"><span className="font-bold">Description:</span> {description}</h1>
        </div>
    );
};

export default AppDescription;